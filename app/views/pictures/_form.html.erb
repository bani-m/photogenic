<p id="notice"><%= notice %></p>

<%= form_for(@picture) do |f| %>
  <%= f.error_messages! %>

<%= form_for(@picture) do |f| %>
  <%= f.label :title %>
  <%= f.text_field :title %>
  <br>
  <%= f.label :content %>
  <%= f.text_field :content %>
  <br>
  <%= f.label :photo_image %>
  <%= f.text_field :photo_image %>
  <%= f.submit %>
<% end %>

<%= link_to "写真一覧画面にもどる", pictures_path %>

<% end %>
